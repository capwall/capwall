// Fallback target for the `#cond-fs` conditional imports entry. Never selected under Node
// (the "node" condition always matches); present so the imports map is well-formed.
export const readFileSync = () => {
  throw new Error("esm-launder-dep: conditional fallback should be unreachable under Node");
};
