// The IMPERSONATED package for issue #84. It is the one package the fixture policy grants
// (env + fs read), and it is completely inert: it exists to have a real path under
// `node_modules/forge-target/` for `forge-dep` to name, and a real frame for the
// "corroborate the eval origin against a frame elsewhere on the stack" experiment.
//
// INERT (AGENTS.md § 8): no capability call of any kind lives in this file.

/** Invoke `fn` so that a genuine `forge-target` frame sits on the stack beneath it. */
function callback(fn) {
  return fn();
}

module.exports = { callback };
