// A LEGITIMATE nested-version-conflict host. The project depends on `legit-nested@1`, which
// npm hoists to the top level; `legit-host` needs `legit-nested@2`, so npm installs a second
// real copy at `node_modules/legit-host/node_modules/legit-nested/`.
//
// This layout is byte-for-byte identical to the #92 attack layout, which is the whole
// difficulty of #92: nothing on disk separates "npm resolved a version conflict" from "an
// attacker shipped a directory with a chosen name". This fixture exists so the fix is measured
// against BOTH.
module.exports = {
  run: () => require("legit-nested")(),
};
