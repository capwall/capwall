// The IMPERSONATION TARGET for issues #92 and #93 — the only top-level package the fixture
// policy grants (`env` + `fs.read`). It is deliberately inert: it exists so that the path
// `<root>/node_modules/forge-target/index.cjs` is a REAL, installed file that an attacker can
// name. Nothing here is ever the code that runs in an attack vector.
module.exports = { real: true };
