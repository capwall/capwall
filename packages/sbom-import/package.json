{
  "name": "@capwall/sbom-import",
  "version": "0.0.0",
  "description": "STRETCH (post-MVP): import CycloneDX SBOM / CBOM into a capwall capabilities.json policy.",
  "license": "MIT",
  "author": "William Zujkowski",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@capwall/policy-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
