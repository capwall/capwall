Placeholder, not a real native addon. capwall's native gate (#49) decides on a `.node` load
BEFORE process.dlopen touches the file, so the CLI round-trip (observe emits `"native": true`,
enforce denies without it) is fully exercisable without a compiler in CI. See
packages/cli/test/cli.test.ts and packages/core/test/native.test.ts.
